<header class="navbar navbar-fixed-top customNav">
      <div class="container">
        <%= link_to "Ourchive", root_url, id: "logo" %>
        <nav>
          <ul class="nav navbar-nav navbar-right">
            <li><%= link_to "Home", root_url, :class => "headerLink"  %></li>
            <% if current_user %>
              <li><%= link_to "Works", works_url %></li>
              <li><%= link_to "Profile", profile_url %></li>
            <% end %>
            <li>  <%= render 'devise/menu/login_items' %></li>
            <li><%= render 'devise/menu/registration_items' %></li>
          </ul>
        </nav>
      </div>
    </header>
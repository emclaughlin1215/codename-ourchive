var work_type;
work_type = $('#work_creation_form_work_type :selected').val();
if (work_type == '1'){
  $("#new_chapters").append("<%= escape_javascript(render partial: 'new_chapter_on_work') %>");
}
else {
  $("#new_chapters").append("<%= escape_javascript(render partial: 'new_external_chapter_on_work') %>");
}

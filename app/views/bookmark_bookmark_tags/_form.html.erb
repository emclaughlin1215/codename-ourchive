<%= form_for(@bookmark_bookmark_tag) do |f| %>
  <% if @bookmark_bookmark_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bookmark_bookmark_tag.errors.count, "error") %> prohibited this bookmark_bookmark_tag from being saved:</h2>

      <ul>
      <% @bookmark_bookmark_tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :bookmark_id %><br>
    <%= f.number_field :bookmark_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

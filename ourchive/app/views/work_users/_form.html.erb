<%= form_for(@work_user) do |f| %>
  <% if @work_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@work_user.errors.count, "error") %> prohibited this work_user from being saved:</h2>

      <ul>
      <% @work_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :work_id %><br>
    <%= f.number_field :work_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
